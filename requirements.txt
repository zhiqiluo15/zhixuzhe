# 智序者核心依赖（面向 Windows 用户）
# Python 3.10+（开发环境为 3.14）
requests>=2.28.0
psutil>=5.9.0
numpy>=1.24.0

# 可选：GPU 算力验证（无 GPU 可跳过，不影响核心功能）
#   pip install torch --index-url https://download.pytorch.org/whl/cu128
#   （RTX 50 系 / CUDA 12.x 请用 cu128 源；其他显卡见 https://pytorch.org）

# 可选：视频制作工具（make_video，无此依赖时该工具懒加载报错，不影响核心功能）
#   edge-tts 云端配音需能访问 speech.platform.bing.com；不通时自动回退本地 SAPI5
edge-tts>=6.1.0
moviepy>=2.0.0
Pillow>=10.0.0
pyttsx3>=2.90

# 可选：美术资源库自动分类（classify_images，无此依赖时该工具懒加载报错，不影响核心功能）
#   首次调用自动下载多语言 CLIP 模型（约 600MB，走国内 HF 镜像 hf-mirror.com）
#   国内源：pip install torch open_clip_torch -i https://pypi.tuna.tsinghua.edu.cn/simple
torch>=2.13.0
open_clip_torch>=3.3.0

# 可选：语义检索（记忆检索向量化，无此依赖时自动降级纯关键词检索，不影响核心功能）
#   首次检索自动下载 BGE 中文模型（约 100MB；下载慢可设 HF_ENDPOINT=https://hf-mirror.com）
#   国内源：pip install sentence-transformers -i https://pypi.tuna.tsinghua.edu.cn/simple
sentence-transformers>=3.0.0
